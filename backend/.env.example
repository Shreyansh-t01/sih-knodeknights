# PostgreSQL connection string
DATABASE_URL=postgresql://username:password@host/database?sslmode=require

# Server settings
PORT=4000
FRONTEND_ORIGIN=http://localhost:5173

# Use true for Neon and most hosted PostgreSQL databases.
POSTGRES_SSL=true
